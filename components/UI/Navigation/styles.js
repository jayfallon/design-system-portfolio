import styled from 'styled-components';

const HeaderNav_SC = styled.nav`
  ul {
    display: flex;
  }

  li {
    margin: 0 2rem;
    padding: 1rem;

    a {
      font-size: 2rem;
    }
  }
`;

export default HeaderNav_SC;
